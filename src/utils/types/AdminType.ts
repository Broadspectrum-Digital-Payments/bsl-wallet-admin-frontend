export type AdminType = {
    externalId?: string,
    name?: string,
    email?: string,
    status?: string,
    userType?: string,
    ghanaCardNumber?: string,
    phoneNumber?: string,
    actualBalance?: number,
    availableBalance?: number,
    bearerToken?: string,
    createdAt?: string,
}