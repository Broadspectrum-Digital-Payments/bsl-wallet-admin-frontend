export type AdminType = {
    externalId?: string,
    name?: string,
    email?: string,
    status?: string,
    userType?: string,
    verificationToken?: string,
    bearerToken?: string,
    createdAt?: string,
}